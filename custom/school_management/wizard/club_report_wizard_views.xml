<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_club_report_wizard_form" model="ir.ui.view">
        <field name="name">club.report.wizard.form</field>
        <field name="model">club.report.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <group>
                        <field name="club_ids" widget="many2many_tags" options="{'no_open': True, 'no_quick_create':True, 'no_create': True}"/>
                    </group>
                    <group>
                        <field name="student_ids" widget="many2many_tags" options="{'no_open': True, 'no_quick_create':True, 'no_create': True}"/>
                    </group>
                </group>
                <footer>
                    <button string="Print"
                            data-hotkey="q"
                            name="action_print_report"
                            type="object"
                            class="print btn-primary o_mail_send">
                    </button>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_print_club_report" model="ir.actions.act_window">
        <field name="name">Club Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">club.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_club_report_wizard_form"/>
        <field name="target">new</field>
    </record>
</odoo>
